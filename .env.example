# Discord bot token (Developer Portal > Bot > Token).
DISCORD_TOKEN=

# Path to the local SQLite database file. Defaults to ./data/discord-space.sqlite.
DATABASE_PATH=./data/discord-space.sqlite

# How often (ms) the background job-notification sweep runs. Defaults to 60000 (1 minute).
JOB_SWEEP_INTERVAL_MS=60000
