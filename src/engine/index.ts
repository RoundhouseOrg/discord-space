/**
 * Engine root: pure game logic, zero Discord imports (docs/05-tech-stack.md). Pure game logic; must never import from
 * src/discord (enforced by eslint.config.mjs and
 * src/engine/layering.test.ts). Not implemented yet — scaffold only.
 */
export {};
